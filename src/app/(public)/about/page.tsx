import { AboutContent } from "@/components/about/AboutContent";

export const metadata = {
  title: "About | BookClub",
  description: "About BookClub reading management service",
};

export default function AboutPage() {
  return <AboutContent />;
}
