import { CookiesContent } from "@/components/legal";

export const metadata = {
  title: "Cookie Policy | BookClub",
  description: "Cookie Policy for BookClub",
};

export default function CookiesPage() {
  return <CookiesContent />;
}
